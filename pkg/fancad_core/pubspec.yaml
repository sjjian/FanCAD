name: fancad_core
description: "Pure-Dart CAD kernel: geometry, document model, transactions and the command registry."
version: 0.1.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.10.3

dependencies:
  collection: ^1.19.0
  meta: ^1.16.0

dev_dependencies:
  test: ^1.25.0
  lints: ^6.0.0
