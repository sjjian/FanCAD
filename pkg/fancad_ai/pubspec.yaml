name: fancad_ai
description: "The AI layer: provider abstraction, the agent loop, document context building and change approval."
version: 0.1.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.10.3

dependencies:
  fancad_core:
    path: ../fancad_core
  fancad_ops:
    path: ../fancad_ops
  http: ^1.2.2
  json_annotation: ^4.9.0
  meta: ^1.16.0

dev_dependencies:
  build_runner: ^2.5.4
  json_serializable: ^6.9.0
  test: ^1.25.0
  lints: ^6.0.0
