name: fancad_test
description: "Shared test harness: table cases, temp dirs, synthetic drawings."
version: 0.1.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.10.3

dependencies:
  fancad_core:
    path: ../fancad_core
  test: ^1.25.0

dev_dependencies:
  lints: ^6.0.0
